version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"  # Backend
      - "4201:4200"  # Frontend
    container_name: kita-recipes-app
    networks:
      - kita-network

networks:
  kita-network:
    driver: bridge
